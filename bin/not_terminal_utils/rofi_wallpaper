#!/usr/bin/env bash

DIR="$HOME/Images/wallpaper/"

if [[ "$ROFI_RETV" == "0" ]]; then
  while IFS= read -r f; do
    echo -e "$f\0icon\x1f$DIR/$f"
  done < <(ls "$DIR")
elif [[ "$ROFI_RETV" == "1" ]]; then
  echo "$DIR$@" >&2
else
  FILE="$DIR/$@"
fi
