#!/bin/env bash

choice=$(printf "Lock\nSuspend\nReboot\nShutdown" | rofi -dmenu)
case "$choice" in
  Lock) hyprlock ;;
  Logout) pkill -KILL -u "$USER" ;;
  Reboot) systemctl reboot ;;
  Shutdown) systemctl poweroff ;;
esac
